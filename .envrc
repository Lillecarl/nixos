export FLAKE=$(pwd)

watch_file $(find . -name "*.nix" -printf "%p ")

IFS=":"

mkdir -p $PWD/.nix
for path in $(nix develop --profile $PWD/.nix/profile -c sh -c "echo $PATH"); do
  PATH_add $path
done

