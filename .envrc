# We need $FLAKE for our impurity
export FLAKE=$(pwd)
# Create profile directory
mkdir -p $PWD/.nix
# Watch for changes in .nix files
watch_file $(find . -type f -name "*.nix" -printf "%p ")
# Add profile to PATH
PATH_add $(nix develop .#repoenv --profile $PWD/.nix/profile -c sh -c 'echo $PATH')
